on:
  push:
    branches:
    - main
    - 'develop/**'
jobs:
  my_job:
    runs-on: self-hosted   
    steps:
    - uses: actions/checkout@v4
    - name: Test
      run: |
       docker run hello-world
