on:
  push:
    branches:
    - main
    - 'develop/**'
jobs:
  my_job:
    runs-on: self-hosted   
    steps:
    - name: Show Docker Version
      run: docker --version
    - name: Test
      run: |
       docker run hello-world
